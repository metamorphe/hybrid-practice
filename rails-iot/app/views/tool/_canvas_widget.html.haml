:javascript
  // var mainVM, mainPaper;
  // $(function(){
  // 	mainPaper = paperSetup('main-canvas');
  // 	mainPaper.name = "mainPaper";	
  // });
  $('.tool')
  //
#main-canvas-container
  %canvas#main-canvas.paper
  #new-selector.drop-tool
    %button.drop-tool-select.btn-ard SELECT TOOL
    %ul
      %li.tool{"data-tool" => "BurstSelectTool"}
        BURST
      %li.tool{"data-tool" => "RiverSelectTool"}
        RIVER
      %li.tool{"data-tool" => "ComponentSelectTool"}
        COMPONENT
:css
  #new-selector{
  	position: absolute;
  	z-index: 1000;
  	top: 30px;
  	left: 30px;
  }
  #new-selector ul li button{
  	width: 100px;
  }

  .paper{
    background: #f5f4f0;
    position: relative;
    top: 0px;
    z-index: 0;
    cursor: pointer;
    -webkit-box-shadow: inset 2px 4px 15px 2px rgba(0,0,0,0.2);
  	-moz-box-shadow: inset 2px 4px 15px 2px rgba(0,0,0,0.2);
  	box-shadow: inset 2px 4px 15px 2px rgba(0,0,0,0.2);
  }
