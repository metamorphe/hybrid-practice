<%= form_for(@visual_block) do |f| %>
  <% if @visual_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visual_block.errors.count, "error") %> prohibited this visual_block from being saved:</h2>

      <ul>
      <% @visual_block.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
